# gradle
.gradle/
build/
out/
classes/
net/

# loom dev runtime
run/
mods/
logs/
config/

# IDE
.idea/
*.iml
*.ipr
*.iws
.settings/
.vscode/
.classpath
.project
*.launch

# generated website (built by concord template)
_site/

# local AI / planning state
.claude/settings.local.json
.claude/projects/
.claude/scheduled_tasks.lock
.plan/

# OS junk
.DS_Store
Thumbs.db

# stray runtime artifacts (crash dumps, build/gametest logs, etc.)
*.log

# Fabric datagen hash cache — timestamps inside these files change every run,
# so committing them would make `./gradlew runDatagen` non-idempotent against
# git. The actual generated assets/data under src/main/generated are committed.
src/main/generated/.cache/
